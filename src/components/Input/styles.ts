import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    height: 56,
    backgroundColor: '#202024', // Cor de fundo do input
    borderRadius: 6,
    color: '#E1E1E6', // Cor do texto digitado
    padding: 16,
    fontSize: 16,
  },
});