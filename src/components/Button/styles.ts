import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    height: 56,
    backgroundColor: '#00875F', // Cor de fundo verde
    borderRadius: 6,
    alignItems: 'center',
    justifyContent: 'center',
    marginTop: 20, // Espaço acima do botão
  },
  title: {
    color: '#FFFFFF', // Texto branco
    fontSize: 16,
    fontWeight: 'bold',
  },
});